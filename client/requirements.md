## Packages
canvas-confetti | High-quality confetti animations for correct answers and high scores
@types/canvas-confetti | TypeScript definitions for canvas-confetti
lucide-react | Already installed, but critical for UI icons
framer-motion | Already installed, used for page transitions and visualizer animations

## Notes
AudioContext must be initialized upon user interaction. The frontend handles the sorting simulation, generating the audio dynamically.
Tailwind config requires 'Outfit' for sans and 'JetBrains Mono' for mono fonts.
Colors use a deep dark mode palette with vibrant neon accents for visual feedback.
